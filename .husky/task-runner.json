{
   "comment-enablePreCommitWith": "dotnet husky set pre-commit -c 'dotnet husky run'",
   "tasks": [
      {
         "name": "Run csharpier",
         "command": "dotnet",
         "args": [ "csharpier", "${staged}" ],
         "include": [ "**/*.cs" ]
      },
   ]
}
